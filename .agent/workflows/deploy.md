---
description: Build, Verify, and Deploy to Vercel
---

1. Run full build and type check
// turbo
npm run build

2. Check for uncommitted changes
git status

3. Stage all changes
git add .

4. Commit changes (User input required for message)
git commit -m "chore: deploy updates"

5. Push to Verce
git push
